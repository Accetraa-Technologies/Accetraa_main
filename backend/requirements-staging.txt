# =============================================================================
# Accetraa Technologies — Staging Requirements
# Target: Render free tier (Python 3.12+, PostgreSQL)
#
# Install: pip install -r requirements-staging.txt
# Excludes: PyMySQL, django-ses, django-storages, boto3 (AWS dependencies).
# Re-add those in requirements.txt for production.
# =============================================================================

# ── Core ──────────────────────────────────────────────────────────────────────
Django==5.0.6
djangorestframework==3.15.2

# ── CORS ──────────────────────────────────────────────────────────────────────
django-cors-headers==4.3.1

# ── Database — Render PostgreSQL ──────────────────────────────────────────────
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# ── Environment ───────────────────────────────────────────────────────────────
python-decouple==3.8

# ── Image handling (admin thumbnails, portfolio uploads) ─────────────────────
Pillow==11.3.0

# ── Static files ──────────────────────────────────────────────────────────────
whitenoise==6.7.0

# ── WSGI server ───────────────────────────────────────────────────────────────
gunicorn==22.0.0

# ─────────────────────────────────────────────────────────────────────────────
# REMOVED — not needed for staging (re-add to requirements.txt for production):
#   PyMySQL==1.2.0          → MySQL driver (production uses RDS MySQL)
#   django-ses==3.5.2       → AWS SES email backend
#   django-storages==1.14.3 → S3 / CloudFront media storage
#   boto3==1.34.131         → AWS SDK
# ─────────────────────────────────────────────────────────────────────────────
